{
	auto_https off
}

:{$PORT:80} {
	root * /srv
	encode gzip zstd
	file_server

	@hashed path_regexp \.[a-f0-9]{8,}\.(js|css|woff2?|png|jpg|svg|webp)$
	header @hashed Cache-Control "public, max-age=31536000, immutable"
}
